Canvas c = Canvas.getCanvas();

Circle circle1 = new Circle();
int width = c.getWidth();
int height = c.getHeight();
int xPos = 0;
int currentXPos = 0;
int yPos = 0;
int currentYPos = 0;
java.util.Random rand = new java.util.Random();
circle1.makeVisible();

int count = 5;

while (count < 5){
   xPos = rand.nextInt(width);
   yPos = rand.nextInt(height);
   currentXPos = circle1.getXCoord();
   currentYPos = circle1.getYCoord();
   circle1.slowMoveHorizontal(xPos - currentXPos);
   circle1.slowMoveVertical(yPos - currentYPos);

   count = count + 1;
}

Circle circle2 = new Circle();
circle2.changeColor("green");
int xPos = 0;
int yPost = 0;
circle2.makeVisible();

int currentXPos = 0;
int currentYPos = 0;

count = 0;

while (count < 5){
	xPos=rand.nextInt(width);
	yPos=rand.nextInt(height);
	
	currentXPos = circle2.getXCoord();
	currentYPos = circle2.getYCoord();
	
	circle2.slowMoveHorizontal(xPos - currentXPos);
   	circle2.slowMoveVertical(yPos - currentYPos);

 	count = count + 1;
	
	int circle_position_value = ((circle2.getYCoord()/50)+((width-circle2.getXCoord())/50));

	if (circle_position_value>30){
		circle2.changeSize(circle_position_value);
	}
}



  
